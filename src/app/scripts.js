/**
 * Set of scripts that should be loaded before the application starts.
 * Usually these are registered as globals, all other dependencies should be provided under `vendors.js`.
 */
import 'zone.js';
import 'reflect-metadata'
import 'tslib';
